import React from 'react';
import Navigation from './Navigation';
import Banner from './Banner';

const Hero = () => (
	<header id="home">
		<Navigation />
		<Banner />
	</header>
);

export default Hero;
